import { Meteor } from 'meteor/meteor';

import Items from '../imports/api/Items'

// If there is some reason that the development database needs to be cleared open 
// terminal in the root directory and type `meteor reset`.  On a Windows machine
// you will need to open the terminal window as administrator.  You may have to 
// run the command twice if there are errors on the first attempt. 
Meteor.startup(() => {
  // code to run on server at startup
});
